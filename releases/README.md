# Pre-built Firefox add-on packages.
#
# These files are committed so you can install without running the build script.
# To rebuild after changes, run from the repo root:
#   npm run package:firefox
#   cp dist/manuscript-submit-assistant-*.xpi releases/
